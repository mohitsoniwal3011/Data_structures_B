package com.company.Hashing;

public class Intro {
    public static void main(String[] args) {
        //Hashing is a technique in which we convert a given key into an address using HashFunction() and
        //then we can use this address for insertion, deletion, display operations

        //It is possible that two different keys produces the same address , this condition is called  "COLLISION".
        //and the keys which produces the same address are called "SYNONYMS".
        //we should choose the HashFunction which is easy to implement as well as minimizes the collision.

    }
}
